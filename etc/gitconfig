[core]
	pager = cat

[color]
	ui = auto

[push]
	default = simple

[init]
	defaultBranch = master

[diff]
	diff-algorithm = histogram

[safe]
	directory = /var/cache/root/git/nixpkgs/.git
	directory = /etc/site/.git